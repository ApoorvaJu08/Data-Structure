#include <stdio.h>
int main()
{
    int i;
    int mat[][3] = { {1, 10}, {2, 20, 200}, {3}, {4, 40, 400} };
}
//Now the number of rows cannot be increased since after initialization compilor knows the row size
